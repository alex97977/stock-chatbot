import AppContainer from './AppContainer';
import AppRouter from './AppRouter';

const App = () => {
  return (
    <AppContainer>
      <AppRouter />
    </AppContainer>
  );
};

export default App;
